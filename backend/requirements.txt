# Web framework — handles HTTP requests/responses
fastapi==0.115.6
uvicorn[standard]==0.34.0

# File uploads in FastAPI
python-multipart==0.0.20

# Settings from .env files
pydantic-settings==2.7.1
email-validator>=2.0.0

# PDF text extraction (digital PDFs)
pymupdf==1.25.3

# OCR for images and scanned PDFs
pytesseract==0.3.13
Pillow==11.1.0
rapidocr-onnxruntime>=1.4.0
numpy>=1.24.0

# Layout-preserving PDF text (tables, structure)
docling>=2.0.0

# Convert PDF pages to images (for scanned PDF OCR)
pdf2image==1.17.0

# Groq LLM (planning / refine)
groq==0.18.0

# OpenAI LLM (primary extraction)
openai>=1.60.0

# JWT auth
python-jose[cryptography]==3.3.0

# Supabase (persistence)
supabase>=2.16.0

# Rate limiting
slowapi==0.1.9

# MIME type detection for uploads
filetype==1.2.0

# LLM retry
tenacity==9.0.0

# Email delivery
resend>=2.0.0

# Google Sheets
google-api-python-client>=2.100.0
google-auth>=2.20.0

# Job queue (Redis + Arq worker)
arq>=0.26.0
redis>=5.0.0

# OpenTelemetry (optional at runtime; enabled via OTEL_ENABLED)
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0

# Tests
pytest==8.3.4
pytest-asyncio==0.25.2
